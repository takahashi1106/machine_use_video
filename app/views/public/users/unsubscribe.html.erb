<div class="container">
  <h2 class="pt-5">退会画面</h2>
    <div class="mt-5">
      <p>退会する場合は、『退会する』をクリックしてください。</p>
    </div>
      <div>
      <%= link_to '退会しない', user_path(current_user), class: "btn btn-outline-info" %>
      <%= link_to "退会する", withdraw_user_path(current_user), method: :patch, "data-confirm" => "本当に退会しますか？", class: "btn btn-outline-danger" %>
      </div>
</div>